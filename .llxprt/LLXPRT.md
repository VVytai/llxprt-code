## LLxprt Code Added Memories
- CRITICAL: NEVER delete, remove, or modify the .llxprt/ directory or any of its contents (LLXPRT.md, settings.json, skills/, commands/). This directory contains project memories, settings, and skills that are version-controlled. Do not run rm, git clean, git checkout, or any other command that would remove these files. If git status shows .llxprt files as modified, leave them alone — they are supposed to be there. "Clean workspace" means only: checkout main and pull from origin. It does NOT mean deleting files.
- Before checking in code changes from the main project directory, run: npm run test, npm run lint, npm run typecheck, npm run format, npm run build, and bun scripts/start.ts --profile-load ollamakimi "write me a haiku and nothing else"; fix any errors, ensure code is formatted before pushing/creating PRs, and always use gh for PRs/issues/comments (never webfetch).
- When user requests to address GitHub issue #NUM: 1) Checkout main and pull latest from origin (do NOT delete any files or run git clean — just git checkout main && git pull), 2) Create branch "issueNUM", 3) Use gh to pull issue and comments, AND assign the issue to acoliver using gh (gh issue edit NUM --add-assignee acoliver), 4) Research issue in codebase using description/comments as starting point, 5) Create test-first plan following dev-docs/RULES.md behavioral tests (no mock theater), 6) Delegate to typescriptexpert subagent with exquisite detail to implement (must run full verification: npm run test/lint/typecheck/format/build and bun scripts/start.ts --profile-load ollamakimi "write me a haiku"), 7) Have deepthinker review for compliance and issue intent (run verification cycle), 8) If fails: remediate with typescriptexpert (giving feedback, run verification), then deepthinker review again - loop until satisfied, 9) Run open code review (ocr) BEFORE pushing: launch detached with `nohup ocr review --audience agent --timeout 20 {{args}} > /tmp/ocr_review.log 2>&1 & echo PID=$!`, then POLL and WATCH for it finishing — repeatedly read /tmp/ocr_review.log and check the PID (`ps -p $PID` or `kill -0 $PID`) with short tool calls until the process is DONE (ALWAYS pass `--timeout 20` for a guaranteed 20-minute floor; NEVER run in the foreground — the shell's ~2-minute process watchdog SIGTERM-kills the foreground process group mid-review, losing all buffered output, and a high timeout_seconds does NOT save it). Ensure tests are NOT filtered — ocr excludes test/spec files by default, so rely on the global `~/.opencodereview/rule.json` include patterns (**/*.test.*, **/*.spec.*, **/__tests__/**) to re-include them; if stdout is lost anyway, recover findings from `~/.opencodereview/sessions/*/*.jsonl` (grep for `code_comment` tool calls). Address every ocr finding: remediate with typescriptexpert, re-run the verification cycle, and re-run ocr if changes were significant - loop until ocr is clean, 10) Add/commit/push and use gh to create PR with exquisite detail including "closes #NUM" or "fixes #NUM", 11) Use gh watch with 5m delay until workflows finish - address coderabbit comments/fix failures (using same subagents), run verification cycle, add/commit/push, watch again - loop until all pass, 12) Use proper gh command syntax (avoid unescaped backticks), 13) NEVER exit to prompt telling user workflows are still running - must watch, loop, fix, watch until complete.
- In llxprt-code, `bun install --frozen-lockfile` is structurally unusable: Bun re-normalizes the lockfile on every pass and fails frozen even immediately after a clean `rm bun.lock && bun install` generation. Renaming the private root package does NOT fix it. Root cause is the monorepo structure (root package, packages/cli, and a self-override all named `@vybestack/llxprt-code`, plus file:../ workspace protocol + 26 overrides). Clean generation (`rm bun.lock && bun install`) IS deterministic (byte-identical SHA across runs, all 16 workspaces incl packages/cli). Plain `bun install` against the committed lockfile works (exit 0, all 16 workspaces resolve, postinstall.cjs Bun-guard exits 0 without triggering npm build). Therefore any Bun CI smoke must use plain `bun install`, NOT --frozen-lockfile. Also: 15/16 workspaces symlink to ../../packages/<x>; packages/cli (@vybestack/llxprt-code) resolves to the published npm package due to the name collision — a known out-of-scope pre-existing issue.
- Issue #2349 planning (agents neutral-type migration) is COMPLETE and execution-ready at project-plans/issue2349/. Plan ID PLAN-20260707-AGENTNEUTRAL: 71 phase files (00,00a,00b,01-33 + NNa siblings), overview.md (1146 lines), specification.md, analysis/domain-model.md + 9 pseudocode files. Went through 8 deepthinker plan-review rounds with a FROZEN review prompt + 8 architect remediations. Critical-count trajectory: 9->4->5->3->5->4->2->3, converged to a steady-state band of 2-4 narrow findings (internal-doc-consistency + verification-precision, no architectural/build-breaking defects). Reviewer confirmed architecturally sound and avoids the #2424 source-swap. Key design decisions locked: streamChunkWrapper.ts + providerStopReason.ts FILE-delete at P25 (writers die earlier, readers survive to P25); sendMessage->ModelOutput flip at P13; AgentExecutionBlockedError.syntheticResponse->blockedOutput:ModelOutput retype at P13; isValidResponse delete P15; getHistory G1/G2 + IContent[] flip at P21 (atomic cross-package, 23 CLI + 5 core consumers); hookWireAdapter.ts single named hook-wire boundary (AST-context allow-listed P07/P13/P31); iContentFromBlocks neutral helper P05; usage-metadata OQ-2u committed to option-C-unconditional (public Gemini-named wire UNCHANGED via usageStatsToPublicUsageMetadata mapper, reasoningTokens preserved INTERNALLY only, public exposure out-of-scope); AST enforcement gate with central versioned allow-list (P02 skeleton checkA/B/C/E/F/G-call real + per-slice hard greps for D/G-barrel/H, full gate P31). Review backups saved at project-plans/issue2349/.plan-review-round{1-8}.md. Remaining R8 findings were all narrow/actionable and were remediated in the final consolidation pass.
